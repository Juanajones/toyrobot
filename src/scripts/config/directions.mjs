'use strict';

// is this entire module file needed?

export default class directions {
    constructor(){}
    
    static cardinalDirections(){
        ['NORTH', 'EAST', 'SOUTH', 'WEST']
    };

    // is this still required?
    // static incrementMap(){
    // return new Map([
    //         [0, ['y', 1]],
    //         [1, ['x', 1]],
    //         [2, ['y', -1]],
    //         [3, ['x', -1]]
    //     ]);
    // }
}